Pre-release

 * change grcregressiontest to take command line options of font files to compare
 * add GDLPP envvar to grcompiler to allow specifying where gdlpp is

Pre 2.5 (the next number from the Windows release series)

 * Working on mac
 * manpages
 
4.4 changes

 * Add cmap entries for pseudo-glyphs with Unicode input specified; or deprecate feature if it gets too hard
 * Handle font with no glyf table; give warning if they ask for bounding box data
 * Automatically create associations for deletion rules

Future ideas

 * Allow attachments in substitution table
 * Compress tables (Glat, Gloc, fsms)


